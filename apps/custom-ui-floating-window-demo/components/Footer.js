export default function Footer() {
  return (
    <div class="alert alert-light text-center note">
      <small>
        Since this is a demo app, no real calls will be placed.You can however,
        fake the interactions using the control centre
      </small>
    </div>
  );
}
